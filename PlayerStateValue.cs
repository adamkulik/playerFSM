/**
 * First value will always be used as invalid one, marking no change
 */
public enum PlayerStateValue
{
    NOT_VALID,
    AIM,
    SHOOT,
    WALK,
    SLIDE,
    JUMP,
    JUMPSHOOT,
    SLIDESHOOT
}